streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0
watchdog>=3.0.0
pyyaml>=6.0
MLB-StatsAPI>=1.7.0
lxml>=5.0.0
google-genai>=1.0.0
anthropic>=0.40.0
# Numerical stack for app.core.meta_calibration (2026-04-15 rewrite).
# RidgeCV lives in sklearn.linear_model; nnls lives in scipy.optimize.
# Pinned to ">=" rather than "==" so uv keeps resolving transparently,
# but these are hard dependencies — the calibrator will ImportError on
# first call if they're missing.
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
